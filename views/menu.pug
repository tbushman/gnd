
div.grid-header.mainHeader
	//#app(style="pointer-events:none;display: inline-block;float: left;margin: 0 auto;width: auto;position: absolute;z-index: 50;height: 33.2%;")

	if (type === 'draw')
		a.mainmenu(href="#", title="Save", id="save")
			img.i(src="/images/icons/check.svg")
	else
		a.dropdown.mainmenu(href="#", title="Menu")
			img.i(src="http://pu.bli.sh/images/menu_button.svg")

	
div.grid-left
	#wrapper
		div#tlmodules.dropdown
			a.module(href="/", title="home")
				.ui
					img(src="/images/icons/home.svg")
				span.tb-font-l Home
			if (loggedin)
				if (menuData)
					if (menuData.publishers[0].username === loggedin)
						if (doc)
							a.module.dropdown(title="edit")
								.ui
									img(src="/images/icons/edit.svg")
								span.tb-font-l Edit

								each key, j in itemkeys
									
									each ing, k in key
										a.module.focus.drop(href="#", onclick="document.getElementById('form_"+key[j]+"_"+k+"').submit();")
											.ui
												img(src=(ing.unlocked ? "/images/icons/"+key[j]+"_"+k+".svg" : "/images/icons/unknown.svg"))

									
										//each ing, k in module
											span.tb-font-l #{ing.name}
											if (ing.unlocked)
												form(id="form_"+keys[j]+"_"+k+"", method="POST", enctype="multipart/form-data", action="/api/selectlayer/"+menuData.urltitle+"/"+menuData.pageindex+"/"+index+"/"+keys[j]+"/"+k+"")
													input(type="hidden", value="1", name= ing.name)
													input(type="hidden", id="inputimg_"+keys[j]+"_"+k+"", name= ing.name, value= ing.image)

									//-
										else
											each ing, k in `#{theStore[keys[j]]}`
												a.module.focus.drop(href="#", onclick="document.getElementById('form_"+keys[j]+"_"+k+"').submit();")
													.ui
														img(src=(ing.unlocked ? "/images/icons/"+keys[j]+"_"+k+".svg" : "/images/icons/unknown.svg"))
													span.tb-font-l #{ing.name}

													form(id="form_"+keys[j]+"_"+k+"", method="POST", enctype="multipart/form-data", action="/api/selectlayer/"+menuData.urltitle+"/"+menuData.pageindex+"/"+index+"/"+keys[j]+"/"+k+"")
														input(type="hidden", value="1", name= ing.name)
														input(type="hidden", id="inputimg_"+keys[j]+"_"+k+"", name= ing.name, value= ing.image)

									
																	
						a.module(href="/logout", title="logout")
							.ui
								img(src="/images/icons/logout.svg")
							span.tb-font-l Log out
			else
				a.module(href="/login", title="login") 
					.ui
						img(src="/images/icons/login.svg")
					span.tb-font-l Login
				
				//-
					a.module(href="/register", title="register") 
						.ui
							i.fa.fa-id-card-o
						span.tb-font-l Register
